import 'package:flutter/material.dart';

class CheckPinPage extends StatelessWidget {
  const CheckPinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Check PIN')),
    );
  }
}
